import { createApp } from "vue";
import App from "./App.vue";

createApp(App).mount("#app");

var boton = document.getElementById('nodo-1');

boton.addEventListener('submit', function (e) {
    console.log()
})
